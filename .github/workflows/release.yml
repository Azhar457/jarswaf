name: Release Precompiled Binary

on:
  release:
    types: [created]
  workflow_dispatch: # Allows manual trigger

permissions:
  contents: write

jobs:
  build-release:
    name: Build and Release Binary
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Rust (Stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install Rust (Nightly for eBPF)
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src

      - name: Install dependencies & bpf-linker
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev build-essential
          cargo install bpf-linker

      - name: Build eBPF Program
        run: cargo +nightly run --package xtask

      - name: Build Release Binary
        run: cargo build --release

      - name: Prepare Assets
        run: |
          mv target/release/aegis-waf aegis-waf-linux-amd64
          cp target/bpfel-unknown-none/release/aegis-ebpf aegis-ebpf

      - name: Upload Binaries to Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            aegis-waf-linux-amd64
            aegis-ebpf
          tag_name: latest
          name: Latest Release
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
